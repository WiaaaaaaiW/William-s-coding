module myFSM(
	input clk,
	input cent_25,
	input cent_50,
	input reset,
	output reg food,
	output reg change,
	output reg [3:0] stateDisplay
);

reg [1:0] state;
parameter S0 = 2'b00,
          S1 = 2'b01,
			 S2 = 2'b10;

// Sequential Block: for transitioning to next state
always @(posedge clk)
begin
    // Handle Reset Logic
	
	
	 // Update next state depending on state
end


// Combinational block: for output logic
always @ *
begin
	stateDisplay <= state;
	{change, food} <= 2'b00;
	
	// Update output depending on state

end

endmodule
